#include<iostream>
using namespace std;
int main(){
    // Largest word in a sentence
    int n;
    cin>>n;
    cin.ignore();
    
    char arr[n+1];
    cin.getline(arr,n);
    cin.ignore();

    int mx = 0;
    int curr = 0;

    for (int i = 0; i < n; i++)
    {
        

        /* code */
    }
    

    return 0;
}